#include <stdio.h>
/**
 * main - entry point
 * Description: print all single digit numbwr of base 10
 * startung from 0, followed by a new line.
 * Return: 0 success
 */
int main(void)
{
	int c = 0;

	while (c < 0)
	{
		putchar(44 + c);
		c++;
	}

	putchar('\n');
	return (0);
}
