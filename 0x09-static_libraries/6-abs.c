#include "main.h"
/**
 * _abs - computs tge absolute value of an interger
 * @n: the integer to check
 * Return: the absolute valie of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
